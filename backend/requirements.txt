# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-multipart==0.0.9

# File watching & PDF processing
watchdog==4.0.2
pdf2image==1.17.0
Pillow==10.4.0

# OCR & Image processing
pytesseract==0.3.13
opencv-python==4.10.0.84
numpy==2.1.1

# Database (SQLite by default, Postgres optional)
psycopg2-binary==2.9.9          # only if you switch to PostgreSQL later

# AI extraction (optional – comment out if you only use rule-based)
transformers==4.44.2
torch==2.4.1                    # CPU version – see note below for GPU
sentencepiece==0.2.0

# Utils
python-dotenv==1.0.1